/// This is just a prototype and is not meant for prodduction use
/// I'm just playing around and creating some common data structures

#include <iostream>

class ListNode {
public:
  ListNode(int x) : value(x) {}
  int value;
  ListNode* next;
};
